services:
  lavalink:
    image: ${LAVALINK_IMAGE:-flash105-lavalink:latest}
    build:
      context: .
      dockerfile: Dockerfile.lavalink
    restart: unless-stopped
    volumes:
      - ./plugins:/opt/Lavalink/plugins
    expose:
      - "2333"

  discord-music-bot:
    image: ${BOT_IMAGE:-discord-music-bot:latest}
    build: .
    env_file:
      - .env
    depends_on:
      - lavalink
    restart: unless-stopped
